function insertLeadingSemicolonIfNeeded(changeTracker: textChanges.ChangeTracker, beforeNode: Node, sourceFile: SourceFile) {
		if (changeTracker.hasLeadingSemicolonIfNeeded(changeTracker)) {
			return;
		}
		if
}